#ifndef SAYCAW_H
#define SAYCAW_H

/**
 * @brief Funkce vypíše "caw" na stdout.
 */
void SayCaw();

/**
 * @brief Funkce vraci číslo, které je funkci dáno jako parametr.
 * @param x Je to číslo, které bude vráceno.
 * @return Vrací číslo, které funkce obdržela.
 */
int GetNumber(int x);

#endif

